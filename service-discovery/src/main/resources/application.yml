server:
  port: 8761

spring:
  application:
    name: discoveryservice

eureka:
  client:
    register-with-eureka: false #현재 서비스가 Eureka 서버에 자동으로 등록되지 않도록 막음
    fetch-registry: false #Eureka 서버에서 레지스트리 정보를 가져오지 않도록 막는 역할
